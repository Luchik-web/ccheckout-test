/**
 * Base Request adapter
 */
import RequestAdapterAbstract from '@comfy/core/src/infrastructure/request/request.adapter.abstract';

class RequestAdapter extends RequestAdapterAbstract { };

export default new RequestAdapter()
